// Given two strings S and T, return if they equal when both are typed out. Any '#' that appears in the string counts as a backspace

// "cb#d" -> "cd"

const typedOutStrings = () => {};

module.exports = typedOutStrings;
